Within lib/content/*, site.json

# types.ts: Typescript types used across app and scripts.

# schema.ts: Zod validation schema (runtime safety).

# read.ts: cached helpers:
    - getSiteContent()
    - getPageByPath(path)

# site.json: canonical ingested content file created by ingest.ts pipeline